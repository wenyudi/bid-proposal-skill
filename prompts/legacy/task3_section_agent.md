你只写 legacy 投标方案的一章。语言 {LANG}；章节 N={N}，标题={TITLE}，子节={SUBS}，建议上限={PER_SECTION_CHARS}。

必须实质回答：{REQUIREMENTS_BLOCK}

可用公开情报：{INTEL_BLOCK}

全案叙事与决策边界：{NARRATIVE_AND_DECISIONS}

不要写章级 `##`；第一行用 `>` 给客户结果主张；子节用 `### N.1`，标题含主张。每节先客户任务，再结论、执行路径、责任/资源/时点、公开证据/验收和自然过渡。创意必须能落地；报价单一最优解；第三方案例不冒充我方能力。

禁止虚构、URL、private 引语、内部 ID/工具/模式/叙事自述、销售 CTA、负偏离式排除项、LaTeX。assumed 不能写成已确认；效果用目标/预期/力争，除非真实授权才保证。写 `{TMPDIR}/sections/section-{N}.md`，回答只返回路径。

---
`proposal legacy`
